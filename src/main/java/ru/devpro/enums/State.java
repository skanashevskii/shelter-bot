package ru.devpro.enums;


import lombok.Getter;

@Getter
public enum State {

        AWAITING_ADDRESS,
        AWAITING_CITY,
        AWAITING_STATE,
        AWAITING_ZIPCODE,
        AWAITING_NAME,
        AWAITING_USER_NAME,
        AWAITING_SAFETY,
        AWAITING_FAMILY,
        AWAITING_ROLE,
        AWAITING_PHONE,
        AWAITING_EMAIL,
        AWAITING_ANIMAL_NAME, AWAITING_TYPE, AWAITING_BREED, AWAITING_ANIMAL_TEXT, AWAITING_ANIMAL_PHOTO,


        // Другие состояния для разных категорий данных
    }

